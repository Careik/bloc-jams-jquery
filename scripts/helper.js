/*{
  class Helper {
    constructor() {
      this.currentlyPlaying = song;
    }
    playPauseAndUpdate(song) {
      player.playPause(song) = this.currentlyPlaying);
      const duration = player.getDuration()
      $('#time-control .total-time').text(duration);
    }
    let helper = new Helper();
  }
};
*/
